{ "scripts": {
    "test": "nyc --reporter=text mocha --exit"
  },
  "devDependencies": {
    "chai": "^4.3.7",
    "chai-http": "^4.3.0",
    "mocha": "^10.2.0",
    "nyc": "^15.1.0"
  },
  "nyc": {
    "exclude": [
      "mock-db",
      "coverage",
      "test",
      "mongo.js"
    ]
  }
}
